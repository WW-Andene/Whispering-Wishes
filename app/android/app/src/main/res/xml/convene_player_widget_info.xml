<?xml version="1.0" encoding="utf-8"?>
<!-- Small, roughly-square player — 2x2 cells, resizable down to 1x1 like
     PulseBannerWidget's own 1-row support (see banner_widget_info.xml's comment
     on the minWidth/minHeight-vs-targetCellWidth/Height distinction). -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:minResizeWidth="70dp"
    android:minResizeHeight="70dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:previewImage="@mipmap/ic_launcher"
    android:initialLayout="@layout/widget_convene_player"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:configure="cc.andene.whisperingwishes.ConvenePlayerConfigureActivity"
    android:description="@string/convene_player_widget_description">
</appwidget-provider>
