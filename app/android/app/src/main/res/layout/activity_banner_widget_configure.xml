<?xml version="1.0" encoding="utf-8"?>
<!-- BannerWidgetConfigureActivity — shown automatically by Android when the
     widget is first placed (declared via android:configure in
     banner_widget_info.xml), and reopened later by the widget's own gear
     icon (widget_settings in widget_banner.xml) to change the choice
     afterward. Plain Views, not RemoteViews — this is a normal Activity.

     configure_grid is populated entirely in Java (BannerWidgetConfigureActivity
     builds one tile per currently-active banner, character and weapon both,
     each with its own art thumbnail) rather than two fixed buttons — the whole
     point of per-widget custom banner choice is that the list of options is
     dynamic (however many banners are actually live right now), not a fixed
     "character vs weapon" pair. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/app_background"
    android:padding="20dp">

    <TextView
        android:id="@+id/configure_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_configure_title"
        android:textColor="#FFFFFF"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/configure_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/widget_configure_unavailable"
        android:textColor="#9CA3AF"
        android:textSize="14sp"
        android:gravity="center"
        android:visibility="gone" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <GridLayout
            android:id="@+id/configure_grid"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnCount="3"
            android:alignmentMode="alignMargins" />
    </ScrollView>

</LinearLayout>
