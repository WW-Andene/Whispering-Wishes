<?xml version="1.0" encoding="utf-8"?>
<!-- CurrencyInputActivity — small floating dialog (AppTheme.WidgetInputDialog) for typing
     a currency value in by hand, since RemoteViews home-screen widgets can't host a real
     EditText themselves. See CurrencyInputActivity's own file header. Reuses
     widget_background (the same dark rounded card every widget in this app falls back to)
     as its own background rather than a new drawable. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="256dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="16dp">

    <TextView
        android:id="@+id/currency_input_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginBottom="12dp" />

    <EditText
        android:id="@+id/currency_input_value"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:maxLines="1"
        android:textColor="#FFFFFF"
        android:textColorHint="#8892a4"
        android:textSize="16sp"
        android:background="@drawable/widget_target_choice_bg"
        android:padding="12dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="16dp">

        <Button
            android:id="@+id/currency_input_cancel"
            style="?android:attr/buttonBarButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@android:string/cancel" />

        <Button
            android:id="@+id/currency_input_save"
            style="?android:attr/buttonBarButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="@android:string/ok" />
    </LinearLayout>
</LinearLayout>
