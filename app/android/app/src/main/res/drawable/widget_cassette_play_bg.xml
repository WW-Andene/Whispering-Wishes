<?xml version="1.0" encoding="utf-8"?>
<!-- Primary play/pause tab — same shape family as widget_cassette_button_bg.xml but a shade
     darker bronze, so it reads as the primary action among the transport row's tabs. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#B4884A" />
    <corners android:radius="8dp" />
    <stroke
        android:width="1dp"
        android:color="#6B4E2A" />
</shape>
