<?xml version="1.0" encoding="utf-8"?>
<!-- Goal gauge fill at >= 80% — 5-star gold, matches kuro.css's own --color-gold token
     (#edaf18), same color widget_target_selected_gold.xml already uses for the Resonator
     target choice. See CalculatorWidget.renderProgressSection for the percent thresholds. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="6dp" />
            <solid android:color="#33FFFFFF" />
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="6dp" />
                <solid android:color="#edaf18" />
            </shape>
        </clip>
    </item>
</layer-list>
