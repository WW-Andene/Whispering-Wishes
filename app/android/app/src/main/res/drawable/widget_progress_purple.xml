<?xml version="1.0" encoding="utf-8"?>
<!-- Goal gauge fill at >= 60% — 4-star purple, matches kuro.css's own --rarity-4star /
     --accent-purple token (#c084fc). See CalculatorWidget.renderProgressSection for the
     percent thresholds. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="6dp" />
            <solid android:color="#33FFFFFF" />
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="6dp" />
                <solid android:color="#c084fc" />
            </shape>
        </clip>
    </item>
</layer-list>
