<?xml version="1.0" encoding="utf-8"?>
<!-- Goal gauge fill at >= 20% — 2-star green, matches kuro.css's own .active-emerald
     (#22c55e), same token widget_target_selected_emerald.xml already uses for the "Both"
     target choice. See CalculatorWidget.renderProgressSection for the percent thresholds. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="6dp" />
            <solid android:color="#33FFFFFF" />
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="6dp" />
                <solid android:color="#22c55e" />
            </shape>
        </clip>
    </item>
</layer-list>
