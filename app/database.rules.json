{
  "rules": {
    "leaderboard": {
      ".read": "auth != null",
      "$entryId": {
        ".write": "auth != null && (!data.exists() || data.child('timestamp').val() + 10000 < now) && (!data.exists() || !data.hasChild('ownerUid') || data.child('ownerUid').val() == auth.uid)",
        ".validate": "newData.hasChildren(['id', 'avgPity', 'pulls', 'totalPulls', 'timestamp', 'ownerUid'])
                      && newData.child('avgPity').isNumber()
                      && newData.child('avgPity').val() >= 1
                      && newData.child('avgPity').val() <= 80
                      && newData.child('pulls').isNumber()
                      && newData.child('pulls').val() >= 0
                      && newData.child('pulls').val() <= 9999
                      && newData.child('totalPulls').isNumber()
                      && newData.child('totalPulls').val() >= 0
                      && newData.child('totalPulls').val() <= 50000
                      && newData.child('timestamp').isNumber()
                      && newData.child('timestamp').val() <= now + 60000
                      && newData.child('id').isString()
                      && newData.child('id').val().length <= 64
                      && newData.child('ownerUid').isString()
                      && newData.child('ownerUid').val() == auth.uid
                      && (!newData.hasChild('version') || (newData.child('version').isString() && newData.child('version').val().length <= 16))"
      }
    },
    "community-pulls": {
      ".read": "auth != null",
      "$entryId": {
        ".write": "auth != null && (!data.exists() || data.child('timestamp').val() + 10000 < now) && (!data.exists() || !data.hasChild('ownerUid') || data.child('ownerUid').val() == auth.uid)",
        ".validate": "newData.hasChildren(['timestamp', 'ownerUid'])
                      && newData.child('timestamp').isNumber()
                      && newData.child('timestamp').val() <= now + 60000
                      && newData.child('ownerUid').isString()
                      && newData.child('ownerUid').val() == auth.uid"
      }
    },
    "presence": {
      ".read": "auth != null",
      "$sessionId": {
        ".write": "auth != null",
        ".validate": "newData.hasChildren(['t'])
                      && newData.child('t').isNumber()
                      && newData.child('t').val() <= now + 60000"
      }
    },
    "user-history": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid",
        ".validate": "newData.hasChildren(['timestamp'])
                      && newData.child('timestamp').isNumber()"
      }
    },
    "$other": {
      ".read": false,
      ".write": false
    }
  }
}
