# Whispering Wishes — Wuthering Waves Companion
# Blocked to all automated crawlers (search engines included). This has no
# effect on the app itself or its assets loading normally in a user's
# browser — robots.txt only governs bots, not real page loads.

User-agent: *
Content-Signal: search=no,ai-train=no,use=no
Disallow: /
