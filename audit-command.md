Read the audit skill files in docs/skills/ before proceeding.

If "$ARGUMENTS" specifies "app", "code", "security", or "full" → read docs/skills/app-audit/SKILL.md and follow it.
If "$ARGUMENTS" specifies "design", "visual", or "aesthetic" → read docs/skills/design-aesthetic-audit/SKILL.md and follow it.
If "$ARGUMENTS" specifies "both" or "companion" → read both skill files and use companion mode.
If "$ARGUMENTS" is empty or ambiguous → ask which audit type: Full App Audit, Design & Aesthetic Audit, or Both.

Then audit the current project following the loaded skill instructions.
