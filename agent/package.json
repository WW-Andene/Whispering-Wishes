{
  "name": "ww-b",
  "version": "2.0.0",
  "type": "module",
  "description": "WW-B: audit-only maintenance agent for Whispering Wishes — checks data, finds bugs, compiles reports",
  "main": "index.js",
  "scripts": {
    "audit": "node index.js",
    "micro": "node index.js --mode=micro",
    "dry-run": "node index.js --dry-run",
    "code-only": "node index.js --only=audit"
  },
  "dependencies": {
    "groq-sdk": "^0.12.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
