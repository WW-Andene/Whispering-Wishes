node_modules/
dist/
# The filtered native-build bundle (see capacitor-build/build.mjs) — regenerated by
# `npm run build:native`. android/'s own .gitignore covers its build artifacts
# (local.properties, .gradle/, build/, the copied assets/public) separately.
app/dist-native/
app/ios/

# Environment files (secrets, API keys)
.env
.env.local
app/.env
app/.env.local
app/self-host/.env

# Agent runtime memory (local to each environment)
agent/.memory.json
agent/node_modules/
agent-next/
agent/.checkpoint.json
agent/.schedule-hint.json
agent/.commit-msg.tmp
agent/.memory.lock
.claude/
app/public/vips-properties.xml
